{
  "name": "ice-committee-registration",
  "version": "1.0.0",
  "description": "ICE Committee Registration Portal - Separated Backend & Frontend",
  "type": "module",
  "scripts": {
    "backend:dev": "cd backend && npm install && npm run dev",
    "backend:start": "cd backend && npm run start",
    "frontend:dev": "cd frontend && npm install && npx http-server -p 3000",
    "setup": "cd backend && npm install && prisma generate && cd ../frontend && npm install"
  },
  "dependencies": {
    "@prisma/client": "5.22.0",
    "axios": "^1.4.0",
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.8.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^2.0.2",
    "zod": "^3.23.2"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  }
}
